- name: install apache & php
  remote_user: ubuntu
  hosts: lampstack
  become: true
  become_user: root
  gather_facts: true
  tasks:
    - name: "Install apache2"
      package: name=apache2 state=present